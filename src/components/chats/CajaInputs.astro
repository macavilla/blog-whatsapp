---

---

<!-- --ACS-- -->

<div class="caja-inputs">
  <div class="contenedor-escribir-mensaje">
    <div class="separador-flex">
      <button class="icono-emoji">🙂</button>
      <input type="text" class="escribir-mensaje" placeholder="Mensaje">
      <button class="icono-emoji">📎</button>
      <button class="icono-emoji">📷</button>
    </div>
  </div>
  <button class="icono-audio">🎙️</button>
</div>

<!-- --CCS-- -->

<style>
  .caja-inputs {
    width: 100%;       /*PARCHEEE*/
    max-width: 100vw;  /*PARCHEEE*/
    max-width: 100svw; /*PARCHEEE*/
    overflow: hidden;  /*PARCHEEE*/
    height: 100%;
    padding: 2px 10px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
  }

  .contenedor-escribir-mensaje {
    flex: 1 5 0;
    height: 100%;
    padding: 4px 14px;
    border: 0;
    border-radius: 40px;
    color: var(--cl-gris-claro-texto);
    background-color: var(--cl-gris-escri-display);
    overflow: hidden;
  }

  .separador-flex {
    height: 100%;
    display: flex;
    flex-wrap: nowrap;
    justify-content: space-between;
    gap: 12px;
  }

  .escribir-mensaje {
    flex: 1 5 0;
    min-width: 50px;
    height: 96%;
    font-size: 1.2rem;
    color: var(--cl-gris-claro-texto);
    background-color: var(--cl-gris-escri-display);
    /* background-color: rgb(120, 100, 100); */
    border-radius: 20px;
    border: 0;
  }

  .icono-emoji {
    flex: 0 0 auto;
    overflow: hidden;
    background-color: inherit;
    color: var(--cl-blanco-sld-texto);
    border-radius: 50%;
    font-size: 1.2rem;
    border: 0;
  }

  .icono-audio {
    flex: 0 0 auto;
    height: 100%;
    overflow: hidden;
    aspect-ratio: 1/1;
    font-size: 1.4rem;
    color: var(--cl-negro-linea-display);
    color: var(--cl-blanco-sld-texto);
    background-color: var(--cl-verde-fondo-texto);
    border-radius: 50%;
    border: 0;
  }
</style>